public with sharing class FXRateRetriever {
    public static Double getCurrentRate(String ratePairing) {
        //this is a mocked impementation which simply returns a random number
        return Math.random();
    }
}
